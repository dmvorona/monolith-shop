apiVersion: v1
kind: Service
metadata:
  name: {{ include "monolith-shop.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "monolith-shop.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "monolith-shop.selectorLabels" . | nindent 4 }}
